CREATE TABLE customer_type (CUSTOMERTYPEID INTEGER NOT NULL, CUSTOMERTYPENAME VARCHAR(40) NOT NULL, PRIMARY KEY (CUSTOMERTYPEID))
CREATE TABLE customer (customerId INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, createrId INTEGER, address VARCHAR(255), customerName VARCHAR(255), customerPhone VARCHAR(255), customerTypeId INTEGER, insertDate DATE, updateDate DATE, PRIMARY KEY (customerId))
CREATE TABLE contact (contactId INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, contactAddress VARCHAR(255), contactName VARCHAR(255), insertDate DATE, phoneNumber VARCHAR(255), updateDate DATE, customer_id INTEGER, PRIMARY KEY (contactId))
ALTER TABLE contact ADD CONSTRAINT contactcustomer_id FOREIGN KEY (customer_id) REFERENCES customer (customerId)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
